// API Configuration
// For local development, use: 'http://localhost:8080'
// For production, replace with your Cloud Run URL
const API_BASE_URL = window.location.origin; // Uses same origin as the page

